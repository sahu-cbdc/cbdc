# dependencies
node_modules/
functions/node_modules/
functions/lib/

# build output
dist/
dist-ssr/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
*.local

# vite temp
.vite/

# TypeScript build info
*.tsbuildinfo

# Cloudflare wrangler
.wrangler/

# Environment variables (never commit these!)
# Firebase web config-এর মতো public-safe মান ছাড়া কোনো secret এখানে নেই;
# server secret (যেমন ImgBB API key) শুধু functions/.env-এ — সেটিও ignore করা.
.env
.env.local
.env.production
*.env

# session memory
.arena/
